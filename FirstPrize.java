class FirstPrize extends Prize {
    FirstPrize(int cycle){
        setDescription("Match all of the 4 digits(1 Number)");
        setCycle(cycle);
        setPrizeMoney(1500);
        setNumber("1st Prize", 1);
    }
}
